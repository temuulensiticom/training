@page
@model loginform.Pages.WelcomeModel
@{
    ViewData["Title"] = "Welcome";
}

<section class="py-4">
    <h1 class="h3">Welcome, @Model.FirstName</h1>
    <p class="text-muted">You are signed in as a standard user.</p>
</section>
